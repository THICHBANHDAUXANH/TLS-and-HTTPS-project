DB_HOST=localhost
DB_PORT=3306
DB_USER=pfm_user
DB_PASSWORD=change-me
DB_NAME=personal_finance_db

SECRET_KEY=change-this-to-a-long-random-secret

# Keep this true when the app is accessed through https://localhost via Nginx.
# Set to false only when testing Flask directly at http://127.0.0.1:5000.
SESSION_COOKIE_SECURE=true
SESSION_COOKIE_HTTPONLY=true
SESSION_COOKIE_SAMESITE=Lax
